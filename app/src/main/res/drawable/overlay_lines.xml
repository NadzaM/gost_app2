<!-- overlay_lines.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Jedna linija -->
    <item android:top="-36dp" android:bottom="4dp">
        <shape android:shape="line">
            <stroke android:width="1.5dp" android:color="@color/color_linija" />
        </shape>
    </item>


    <!-- Druga linija -->
    <item android:top="36dp" android:bottom="-4dp">
        <shape android:shape="line">
            <stroke android:width="1.5dp" android:color="@color/color_linija" />
        </shape>
    </item>

</layer-list>
